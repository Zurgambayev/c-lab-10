#include <iostream>
#include <vector>
using namespace std;

bool comp(int k, int l){
    if(k.first)
}

int main(){
    int n; cin >> n;
    vector<pair<int, int>> v;
    for(int i = 0; i < n; i++){
        int k,l; cin >> k >> l;
        v[i] = make_pair(k,l);
    }
}

